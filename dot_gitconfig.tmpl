[user]
	signingkey = 5A0B21C6B9FBE670
	name = {{ .name }}
	email = {{ .email }}
[pull]
	rebase = true
[init]
	defaultBranch = main
[commit]
	gpgsign = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true